# gRPC server for ApplicationService (GetApprovedLeaves).
# After running: python -m grpc_tools.protoc -I proto --python_out=src/grpc_server --grpc_python_out=src/grpc_server proto/application.proto
# implement the servicer in server.py and start the server in main or a separate process.
